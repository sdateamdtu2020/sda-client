import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	dashboard: {
		display: "flex",
		flexDirection: "row",
		overflow: "hidden",
		marginLeft: "2px",
		marginRight: "2px",
		marginBottom: "2px",
	},
}));
