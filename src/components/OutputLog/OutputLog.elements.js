import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	outputLog: {
		width: "100%",
		overflowY: "auto",

		marginTop: "10px",
		marginBottom: "10px",
	},
	button: {
		marginBottom: "10px",
	},
}));
